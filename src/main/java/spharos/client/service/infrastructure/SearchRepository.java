package spharos.client.service.infrastructure;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Repository;
@Repository
@RequiredArgsConstructor
public class SearchRepository {

//    private final JPAQueryFactory queryFactory;

//    @Transactional
//    public List<SearchServiceDto> searchResultList(ServiceBaseCategoryType searchServiceType, LocalDate date,int region){
//        queryFactory.select(services)
//                .from(services,serviceImage) // services를 기준으로 두 테이블 join
//                .where(services.seviceCategory.baseCategory.eq(searchServiceType)
//                        .and(services.serviceArea.areaCode.in(region))
//                        .and(services.)
//
//                .fetch();
//    }
}
